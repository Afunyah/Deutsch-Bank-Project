
import { createTheme } from '@mui/material/styles';

const appTheme = createTheme({
    palette: {
        primary: {
            main: "#0018A8"
        },
        secondary: {
          main: "#5743DB"
        },
        tertiary : {
            main: "#FFFFFF" 
        }
    }
});

export default appTheme;